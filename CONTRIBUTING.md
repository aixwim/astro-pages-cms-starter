# Contributing

Use a focused branch and keep content migrations with the code that consumes them.

```bash
npm ci
npm run format:check
npm run build
```

Do not commit `.env`, credentials, `dist`, or unreviewed Drive exports. Check mobile layouts and keyboard navigation before review.
